// Blank seed template.
//
// This build ships with NO pre-loaded data source and NO results. Pick your own
// source (a subreddit / page / channel) from the Admin Panel, then run a crawl
// to generate archetypes from that source's own content.
//
// To pre-seed a source programmatically instead, import { db, subredditsTable }
// from "@workspace/db" and insert your own row below.

console.log("No demo data seeded. Add your own source from the Admin Panel to begin.");
process.exit(0);
